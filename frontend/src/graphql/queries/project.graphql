query Project($id: Int!) {
  project(id: $id) {
    id
    title
    headline
    headlineImage
    description
    featuredImage
    points
    voteStatus
    category
    creatorId
    creator {
      id
      firstName
      lastName
      email
    }
    createdAt
    updatedAt
  }
}
