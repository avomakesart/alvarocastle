query Experiences($limit: Int!, $cursor: String) {
  experiences(limit: $limit, cursor: $cursor) {
    experience {
      id
      company
      position
      period
      description
      createdAt
      updatedAt
    }
    hasMore
  }
}
