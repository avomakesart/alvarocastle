mutation CreateProject($input: ProjectInput!) {
  createProject(input: $input) {
    id
    title
    headline
    headlineImage
    featuredImage
    description
    voteStatus
    points
    category
    creatorId
    creator {
      id
      firstName
      lastName
      email
    }
    createdAt
    updatedAt
  }
}
